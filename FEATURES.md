# 视频学习辅助智能体 - 完整功能清单

## 项目核心理念
**减少学习时的注意力分散，让用户保持沉浸式学习流（Flow State）**

---

## 🎯 一、核心功能（必须实现）

### 1.1 视频管理模块

#### 1.1.1 视频上传（必须）
- **功能描述**：支持本地 MP4 视频文件上传
- **交互流程**：
  1. 用户点击上传按钮或拖拽文件到页面
  2. 显示上传进度条
  3. 上传成功后自动隐藏/折叠上传控件
  4. 触发后台视频分析任务
- **技术要点**：
  - FastAPI 文件上传接口
  - 前端进度显示
  - 文件大小限制（建议 500MB）
- **验收标准**：能成功上传视频并在播放器中播放

#### 1.1.2 视频播放器（必须）
- **功能描述**：支持视频播放、暂停、进度控制、时间跳转
- **核心能力**：
  - ✅ 基础播放控制（播放/暂停/进度条）
  - ✅ **时间跳转功能**（点击笔记时间戳时自动跳转）
  - ✅ 显示当前播放时间
  - ✅ 音量控制
  - ✅ 全屏支持
- **技术要点**：
  - 使用 video.js 或 HTML5 video 标签
  - 通过 `currentTime` 属性实现跳转
  - 提供 JavaScript API 供其他模块调用
- **验收标准**：
  - 视频能正常播放
  - 点击笔记时间戳能准确跳转到对应位置

#### 1.1.3 视频理解（必须）
- **功能描述**：上传视频后自动调用 AI 生成视频的整体理解
- **处理内容**：
  1. 提取视频关键帧（每 30 秒一帧）
  2. 生成视频字幕（Whisper）
  3. 调用 Claude/GPT-4V 理解视频内容
  4. 生成：
     - 视频主题和摘要
     - 知识点列表
     - 建议的章节划分
- **技术要点**：
  - moviepy 提取关键帧
  - faster-whisper 生成字幕
  - Claude API 进行内容理解
  - 异步任务处理（避免阻塞）
- **验收标准**：上传后 1-3 分钟内返回视频理解结果

---

### 1.2 智能笔记模块

#### 1.2.1 快速标记模式（必须）⭐
- **功能描述**：观看时快速打标记，不打断学习流
- **交互流程**：
  1. 用户按**空格键长按 2 秒**（或点击浮动按钮）
  2. 不暂停视频，在当前时间点创建标记
  3. 屏幕边框短暂闪烁（0.3 秒）+ 提示音（可选）
  4. 右侧时间轴显示彩色标记点
- **标记类型**：
  - 🔴 **红色**：重要知识点（默认）
  - 🟡 **黄色**：疑问/不理解
  - 🟢 **绿色**：补充/灵感
  - 用户可在标记后选择类型（不阻断观看）
- **技术要点**：
  - 键盘事件监听（长按检测）
  - CSS 动画反馈
  - 数据结构：`{timestamp, type, note: ""}`
- **验收标准**：
  - 长按空格 2 秒能创建标记
  - 有明显的视觉反馈
  - 标记点显示在时间轴上

#### 1.2.2 时间戳笔记编辑（必须）
- **功能描述**：在标记点上补充文字笔记
- **交互流程**：
  1. 点击右侧标记点进入编辑模式
  2. 支持 Markdown 格式输入
  3. 实时保存（失焦或按 Ctrl+S）
- **显示格式**：
  - 卡片式展示
  - 显示时间戳按钮（可点击跳转）
  - Markdown 渲染
- **技术要点**：
  - Markdown 编辑器（react-markdown 或 MDXEditor）
  - 自动保存机制
- **验收标准**：
  - 笔记能正常编辑和保存
  - Markdown 能正确渲染
  - 时间戳按钮能触发视频跳转

#### 1.2.3 笔记导出（必须）
- **功能描述**：将所有笔记导出为 Markdown 文件
- **导出格式**：
  ```markdown
  # 视频笔记：[视频标题]

  ## 视频信息
  - 时长：XX:XX
  - 生成时间：YYYY-MM-DD

  ## 知识点概览
  [AI 生成的知识点列表]

  ## 详细笔记

  ### [00:30] 重要知识点
  笔记内容...

  ### [02:15] 疑问
  笔记内容...
  ```
- **技术要点**：
  - 前端生成 Markdown 文本
  - 使用 Blob API 下载文件
- **验收标准**：能成功下载 .md 文件且格式正确

---

### 1.3 AI 助手模块

#### 1.3.1 视频帧截图（必须）
- **功能描述**：捕获当前播放画面
- **交互流程**：
  1. 用户点击"📸 解析当前画面"按钮
  2. 系统截取当前帧
  3. 显示截图预览
- **技术要点**：
  - Canvas API: `ctx.drawImage(videoElement, 0, 0)`
  - 转换为 base64 或 Blob
- **验收标准**：能准确截取当前播放画面

#### 1.3.2 上下文感知问答（必须）⭐
- **功能描述**：对视频画面提问，AI 结合上下文回答
- **上下文包含**：
  1. **当前帧截图**
  2. **前后 30 秒字幕**
  3. **视频整体理解**（章节、主题）
  4. **用户已有笔记**（同一时间段附近的）
- **交互流程**：
  1. 用户截图后输入问题（如"这个公式怎么理解？"）
  2. 系统自动收集上述上下文
  3. 调用 AI API（Claude/GPT-4V）
  4. **流式返回**答案（逐字显示）
  5. 用户可选择将答案插入到笔记中
- **技术要点**：
  - WebSocket 实现流式响应
  - 后端整合多源上下文
  - Anthropic/OpenAI Streaming API
- **验收标准**：
  - AI 能理解"这里"、"这个"等指代
  - 答案与当前画面相关
  - 回复速度流畅（逐字显示）

#### 1.3.3 等待时的微交互（必须）⭐
- **功能描述**：API 调用时提供友好的等待体验
- **实现方式**：

  **方案 A：进度可视化**
  ```
  🔄 正在分析视频内容...
  ├─ ✅ 提取音频（1.2s）
  ├─ ✅ 语音转文字（3.5s）
  ├─ ⏳ 理解画面语义（预计 5s）
  └─ ⏸️  生成知识图谱
  ```

  **方案 B：智能填充内容**
  - 显示刚才截取的关键帧
  - 显示同时间段的字幕
  - "小提示"：相关知识点回顾

  **方案 C：骨架屏 + 动画**
  - 模拟答案逐渐显示的骨架屏
  - 流畅的加载动画

- **可中断性**：用户可以取消当前请求
- **技术要点**：
  - 前端状态管理
  - CSS 动画 / Lottie 动画
  - 后端返回处理进度（可选）
- **验收标准**：
  - 等待时有明显的视觉反馈
  - 用户不会感到焦虑
  - 能够取消操作

---

### 1.4 布局与交互

#### 1.4.1 左右分栏布局（必须）
- **布局比例**：左侧 70%（视频），右侧 30%（笔记/AI）
- **可调节**：拖拽中间分隔线调整比例
- **响应式**：小屏幕自动切换为上下布局（可选）
- **技术要点**：
  - CSS Grid 或 Flexbox
  - react-resizable-panels 库
- **验收标准**：拖拽流畅，布局不崩溃

#### 1.4.2 Tab 切换（必须）
- **两个 Tab**：
  - 📝 **智能笔记流**
  - 🤖 **AI 视界助手**
- **状态保持**：切换 Tab 后数据不丢失
- **技术要点**：React 状态管理
- **验收标准**：切换顺滑，数据持久

#### 1.4.3 iOS 风格 UI（必须）
- **设计原则**：
  - 简洁、留白充足
  - 卡片式设计（圆角、阴影）
  - 柔和的配色（浅色为主）
  - 流畅的动画过渡
- **参考**：Apple Human Interface Guidelines
- **技术要点**：Tailwind CSS + 自定义主题
- **验收标准**：整体观感接近 iOS 应用

---

## 🌟 二、增强功能（加成项）

### 2.1 专注模式（加成）⭐⭐⭐
- **功能描述**：隐藏右侧面板，纯净观看
- **触发方式**：
  - 按 `F` 键切换
  - 或点击顶部按钮
- **效果**：
  - 右侧面板收起（只留细边）
  - 视频区域扩展至全屏
  - 快速标记功能仍可用（通过键盘）
  - 可显示悬浮字幕（可选）
- **退出**：再按 `F` 或点击细边
- **优先级**：⭐⭐⭐ 高（提升沉浸感）

### 2.2 笔记自动整理（加成）⭐⭐⭐
- **功能描述**：视频看完后，AI 自动整理笔记
- **生成内容**：
  1. **结构化笔记**：按章节整理标记点
  2. **知识点清单**：提取关键概念
  3. **待解决问题**：汇总黄色标记（疑问）
  4. **学习建议**：下一步应该做什么
- **触发时机**：
  - 视频播放完毕后自动触发
  - 或用户手动点击"整理笔记"
- **展示方式**：
  - 生成新的"整理后笔记"标签页
  - 用户可在此基础上编辑
- **技术要点**：
  - 调用 AI 分析所有标记 + 笔记
  - 结合视频理解信息
  - Markdown 格式输出
- **优先级**：⭐⭐⭐ 高（核心价值功能）

### 2.3 回顾模式（加成）⭐⭐
- **功能描述**：只播放标记的时间点，快速复习
- **交互流程**：
  1. 用户点击"回顾模式"按钮
  2. 视频自动跳到第一个标记点
  3. 播放前后各 10 秒（可调节：5s/10s/15s）
  4. 播放完自动暂停
  5. 用户确认（点击"下一个"或按空格）
  6. 跳到下一个标记点
- **筛选功能**：
  - 可选择只回顾某种类型（如只看红色标记）
  - 可选择时间范围（如只看前 10 分钟的）
- **技术要点**：
  - 视频跳转逻辑
  - 播放状态管理
- **优先级**：⭐⭐ 中（复习场景很有用）

### 2.4 智能暂停建议（加成）⭐
- **功能描述**：AI 检测到信息密度大时提示暂停
- **触发条件**：
  - 语速突然加快
  - 出现复杂公式、代码
  - 画面变化频繁
- **提示方式**：
  - 左下角非模态提示："内容较复杂，是否暂停？"
  - 3 秒后自动淡出
  - 用户可一键暂停或忽略
- **设置**：可在设置中关闭此功能
- **技术要点**：
  - 实时分析字幕语速
  - OCR 检测画面中的公式/代码
  - 前端非阻塞式提示
- **优先级**：⭐ 低（锦上添花，但实现复杂）

### 2.5 实时字幕增强（加成）⭐⭐
- **功能描述**：显示滚动字幕，支持点击跳转
- **显示方式**：
  - 右侧面板底部显示最近 15 秒字幕
  - 或悬浮在视频上方（半透明）
- **增强功能**：
  - **关键词高亮**：AI 识别的重要术语用不同颜色
  - **点击跳转**：点击某句话，视频跳到对应位置
  - **搜索功能**：搜索字幕内容，快速定位
- **技术要点**：
  - WebVTT 字幕格式
  - 关键词提取（NLP 或 AI）
  - 实时同步显示
- **优先级**：⭐⭐ 中（提升可用性）

### 2.6 键盘快捷键（加成）⭐⭐⭐
- **功能描述**：全键盘操作，减少鼠标使用
- **快捷键列表**：
  ```
  空格（短按）：播放/暂停
  空格（长按 2s）：快速标记
  Q：快速提问（打开 AI 输入框）
  S：截图当前帧
  ← / →：快退/快进 10 秒
  ↑ / ↓：音量增/减
  [ / ]：慢速/快速播放（0.75x / 1.5x）
  N：聚焦笔记编辑框
  F：专注模式
  Esc：关闭所有弹窗/退出专注模式
  Ctrl+S：保存笔记
  Ctrl+E：导出笔记
  ```
- **提示**：首次使用时显示快捷键列表
- **技术要点**：
  - 全局键盘事件监听
  - 防止冲突（如输入框中不触发）
- **优先级**：⭐⭐⭐ 高（显著提升效率）

---

## 🔧 三、技术基础功能

### 3.1 用户设置（可选）
- **可配置项**：
  - AI 模型选择（Claude / GPT-4）
  - 回答风格（简洁/详细、专业/通俗）
  - 快捷键自定义
  - 自动保存频率
  - 智能暂停建议开关
- **技术要点**：LocalStorage 存储
- **优先级**：⭐ 低（Demo 可硬编码）

### 3.2 知识库上传（可选）
- **功能描述**：用户上传课件、文档作为补充上下文
- **支持格式**：PDF, DOCX, TXT, Markdown
- **处理流程**：
  1. 文本提取
  2. 向量化（Embedding）
  3. 存入向量数据库
  4. AI 回答时检索相关内容
- **技术要点**：
  - PyPDF2 / python-docx 提取文本
  - OpenAI Embeddings
  - ChromaDB 向量存储
  - RAG 架构
- **优先级**：⭐ 低（实现复杂，Demo 可省略）

### 3.3 数据持久化（必须简化版）
- **必须保存**：
  - 笔记数据（标记 + 文本）
  - 视频信息（路径、标题、理解结果）
- **Demo 方案**：
  - 使用 JSON 文件存储（`data/notes.json`）
  - 或轻量级 SQLite
- **生产方案**：PostgreSQL + Redis
- **优先级**：⭐⭐⭐ 高（核心功能依赖）

---

## 📊 四、功能优先级总结

### P0（必须实现 - MVP）
1. ✅ 视频上传与播放
2. ✅ 视频理解（上传后分析）
3. ✅ 快速标记模式
4. ✅ 时间戳笔记编辑
5. ✅ 时间跳转（点击笔记跳转）
6. ✅ 视频帧截图 + 上下文问答
7. ✅ 等待时的微交互
8. ✅ 笔记导出
9. ✅ 基础布局（左右分栏 + Tab 切换）

**预计工作量**：2-3 周

### P1（强烈建议 - 显著提升体验）
1. ⭐⭐⭐ 键盘快捷键
2. ⭐⭐⭐ 专注模式
3. ⭐⭐⭐ 笔记自动整理
4. ⭐⭐ 回顾模式
5. ⭐⭐ 实时字幕增强

**预计工作量**：额外 1 周

### P2（锦上添花 - Demo 可选）
1. ⭐ 智能暂停建议
2. ⭐ 用户设置
3. ⭐ 知识库上传

**预计工作量**：额外 1 周

---

## 🎯 五、Demo 演示脚本建议

### 场景：学生观看算法课视频

1. **开场**（0-1 分钟）
   - 上传视频
   - 展示 AI 分析进度（等待微交互）
   - 显示生成的视频摘要和章节

2. **观看 + 快速标记**（1-3 分钟）
   - 正常播放视频
   - 遇到重点时长按空格标记（红色）
   - 遇到疑问时标记（黄色）
   - 展示标记点在时间轴上的显示

3. **专注模式**（3-4 分钟）
   - 按 F 进入专注模式
   - 继续标记
   - 再按 F 退出

4. **AI 提问**（4-6 分钟）
   - 暂停视频
   - 点击"解析当前画面"
   - 输入问题："这个时间复杂度是怎么算出来的？"
   - 展示流式回答
   - 将答案添加到笔记

5. **补充笔记**（6-7 分钟）
   - 点击之前的标记点
   - 补充文字说明
   - 展示 Markdown 渲染

6. **回顾模式**（7-8 分钟）
   - 视频看完
   - 点击"回顾模式"
   - 自动播放所有标记点

7. **笔记整理**（8-9 分钟）
   - 点击"自动整理"
   - 展示 AI 生成的结构化笔记
   - 导出 Markdown 文件

8. **总结**（9-10 分钟）
   - 展示快捷键列表
   - 对比传统笔记方式的优势

---

## 📁 六、配套文档清单

- ✅ `FEATURES.md`（本文档）- 功能清单
- 📝 `TECH_STACK.md` - 技术栈详细说明
- 📝 `API_DESIGN.md` - 后端 API 设计
- 📝 `UI_MOCKUP.md` - 界面设计稿
- 📝 `DEVELOPMENT_PLAN.md` - 开发计划与时间表
- 📝 `DEMO_SCRIPT.md` - 演示脚本

**下一步建议**：
1. 确认 P0 功能范围
2. 设计数据库 Schema
3. 搭建项目框架
4. 实现核心功能
